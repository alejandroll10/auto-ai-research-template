
## Faithful-mode contract pointer (this is a `--faithful` run)

The user has commissioned a faithful implementation of the seed at `output/seed/`. **Before producing output, read `output/seed/mechanism_contract.md`** — its named mechanism, structural invariants, theorem-statement constraints, identification strategy, and stated contribution are non-negotiable. Your output must respect every structural invariant in the contract. **If `mechanism_contract.md` does not exist, or exists but is empty / whitespace-only, do not produce output.** Write a single-line note to `output/seed/missing_contract.md` — `"mechanism_contract.md not found or empty; seed_triage Step 0 must run before this agent can proceed"` — and stop. Do not produce output until the contract file exists and is non-empty.

If a contract invariant cannot be honored due to a genuine correctness constraint (proof unrepairable, identification infeasible, claim mathematically false), append the issue to `output/seed/limitations.md` and proceed with the contract-respecting alternative. Do not produce output that silently violates the contract — the user prefers a paper documenting an honest impossibility over one that quietly substitutes a different mechanism. Additions on top of the contract (extra theorems, comparative statics, robustness checks, deeper characterizations) are encouraged once the contract is faithfully implemented and the contract's mechanism remains the headline.
